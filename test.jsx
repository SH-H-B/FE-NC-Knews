// let fullUrl = null;

// if (topicFilter !== null && topicFilter !== "-1")
//   fullUrl = `${url}/articles?topic=${topicFilter}`;
// else fullUrl = `${url}/articles`;

// return axios.get(fullUrl).then(({ data: { articles } }) => {
//   return articles;
// });
